# Optional: absolute path for markdown + uploads (Docker volume)
# DATA_DIR=/data

# Default lat/lon for weather when a run has no GPS route (Open-Meteo, no API key).
# Falls back to the centroid of the first route in data/routes/, then 52.35 / 5.63 (NL).
# DEFAULT_LAT=52.35
# DEFAULT_LON=5.63
